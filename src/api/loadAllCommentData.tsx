import data from '../data/data.json'

export const getAllCommentData = () => {
    return data
}

export const setDataToLocalStorage = (name : string, data: any) => {
    // localStorage.setItem(name, data)
}